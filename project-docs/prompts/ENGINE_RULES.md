# ENGINE RULES

## 一、绝对红线

- 禁止修改已有正常中文
- 禁止触碰首页/导航/非授权模块
- 禁止批量替换字符串
- 禁止修改编码
- 禁止接入 AI/OCR/语音/网络请求
- 禁止写入数据库或临时库
- 禁止修改未授权文件

---

## 二、开发顺序

F1（主入口）
→ F2（结构输入）
→ F3（绑定）
→ F4/F5（校验/排查）

禁止跨阶段开发。

---

## 三、执行原则

- 每次只改一个函数
- 每次只做一个目标
- 修改必须可回滚
- 不允许顺手优化